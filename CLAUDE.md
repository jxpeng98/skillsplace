# Claude Instructions

Follow the shared repository rules in `AGENTS.md`.

This repository may contain `.claude-plugin/marketplace.json` for Claude Code marketplace metadata. Do not add Claude Code project skills under `.claude/skills/` while it remains catalog-only.
