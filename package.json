{
  "name": "skillsplace-marketplace",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A platform-neutral marketplace repository for agent skills and Codex plugins.",
  "license": "UNLICENSED",
  "scripts": {
    "test": "node --test",
    "sync:qiongli": "node scripts/sync-qiongli-releases.mjs",
    "sync:skills": "node scripts/sync-skills-repo.mjs --source-root ../skills --target-root .",
    "validate": "node scripts/validate.mjs && node --test"
  },
  "engines": {
    "node": ">=18"
  }
}
